import {
  Entity,
  Column,
  CreateDateColumn,
  PrimaryColumn,
} from 'typeorm'

import { v4 as uuid } from 'uuid'

@Entity('{{lowerCase name}}')
export class {{pascalCase name}} {
  @PrimaryColumn()
  id: string

  @Column()
  field1: string

  @Column()
  field2: string

  @Column()
  field3: string

  @CreateDateColumn()
  created_at: Date

  constructor() {
    if (!this.id) {
      this.id = uuid()
    }
  }
}
