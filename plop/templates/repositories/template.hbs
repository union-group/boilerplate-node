import { getRepository, Repository } from 'typeorm'

import { I{{pascalCase name}}DTO } from '../../dtos/I{{pascalCase name}}DTO'
import { {{pascalCase name}} } from '../../entities/{{pascalCase name}}Entity'
import { I{{pascalCase name}}sRepository } from './I{{pascalCase name}}Repository'

export class {{pascalCase name}}Repository implements I{{pascalCase name}}Repository {
  private readonly repository: Repository<{{pascalCase name}}>

  constructor() {
    this.repository = getRepository({{pascalCase name}})
  }

  async lorenIpsum(name: string) {}
}
